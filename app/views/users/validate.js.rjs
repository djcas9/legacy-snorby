if @user.valid?
  #page[:user_session_submit].enable
else
  #page[:user_session_submit].disable
end

page.select('.error_message').each(&:remove)
@user.errors.each do |name, msg|
  page.insert_html :after, "user_#{name}", "<span class='new_user_error_message'>#{image_tag('other/slash.png', :class => 'on_error_icon')} #{msg}</span>"
end