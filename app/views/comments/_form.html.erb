<% form_for @comment do |f| %>
  <%= f.error_messages %>
	<%= f.label :body, "New Comment" %><br />
	<%= f.text_area :body %>
  <br />
  <p>
    <%= f.submit "Submit" %>
    <%= f.submit "Cancel", :onclick => 'history.go(-1);return false;' %>
  </p>
<% end %>
